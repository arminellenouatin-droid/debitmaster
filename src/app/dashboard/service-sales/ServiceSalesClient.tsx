"use client";
// DebitManager Power service sales: focused service cashier, no product stock, activity-scoped data.
import { FormEvent, useEffect, useMemo, useState } from "react";

type Service = { id: string; name: string; price_xof: number; billing_unit: string; activity_id: string };
type Sale = { id: string; customer_name: string; quantity: number; unit_price_xof: number; total_amount_xof: number; payment_method: string; membership_expires_at: string | null; duration_minutes: number | null; created_at: string };
const defaults: Record<string, Array<{ name: string; price: number; unit: string }>> = { GYM: [{ name: "Séance", price: 500, unit: "UNIT" }, { name: "Abonnement mensuel", price: 8200, unit: "MONTH" }, { name: "Tapis roulant", price: 500, unit: "5 MINUTES" }, { name: "Vibromasseur", price: 300, unit: "3 MINUTES" }], LAVAGE: [], LODGING: [] };
const money = (value: number) => `${new Intl.NumberFormat("fr-FR").format(value)} XOF`;
export function ServiceSalesClient({ tenantId, activityCode }: { tenantId: string; activityCode: "GYM" | "LAVAGE" | "LODGING" }) {
  const [services, setServices] = useState<Service[]>([]); const [sales, setSales] = useState<Sale[]>([]); const [serviceId, setServiceId] = useState(""); const [customerName, setCustomerName] = useState(""); const [quantity, setQuantity] = useState("1"); const [price, setPrice] = useState(""); const [expires, setExpires] = useState(""); const [duration, setDuration] = useState(""); const [error, setError] = useState(""); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false);
  async function load() { const [serviceResponse, salesResponse] = await Promise.all([fetch(`/api/power/services?tenantId=${tenantId}`, { cache: "no-store" }), fetch(`/api/power/service-sales?tenantId=${tenantId}&activityCode=${activityCode}`, { cache: "no-store" })]); const serviceResult = await serviceResponse.json(); const salesResult = await salesResponse.json(); if (!salesResponse.ok) throw new Error(salesResult.error ?? "Impossible de charger les ventes du service."); const all = (serviceResult.services ?? []) as Service[]; const own = all.filter((item) => item.activity_id); setServices(own); setSales(salesResult.sales ?? []); if (!serviceId && own[0]) { setServiceId(own[0].id); setPrice(String(own[0].price_xof)); } }
  useEffect(() => { load().catch((cause) => setError(cause instanceof Error ? cause.message : "Impossible de charger les ventes.")); }, [tenantId, activityCode]);
  const fallback = defaults[activityCode] ?? []; const selected = services.find((item) => item.id === serviceId); const total = (Number(quantity) || 0) * (Number(price) || 0); const label = activityCode === "GYM" ? "Salle de GYM" : activityCode === "LAVAGE" ? "Lavage" : "Auberge";
  function chooseService(id: string) { setServiceId(id); const item = services.find((service) => service.id === id); if (item) setPrice(String(item.price_xof)); }
  async function submit(event: FormEvent) { event.preventDefault(); setError(""); setMessage(""); setBusy(true); try { const response = await fetch("/api/power/service-sales", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ tenantId, activityCode, serviceId: serviceId || undefined, customerName, quantity: Number(quantity), unitPriceXof: Number(price), membershipExpiresAt: expires ? new Date(`${expires}T23:59:59`).toISOString() : undefined, durationMinutes: duration ? Number(duration) : undefined }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error ?? "Impossible d’enregistrer la vente."); setSales((current) => [result.sale, ...current]); setCustomerName(""); setMessage("Vente enregistrée dans la caisse de l’activité."); } catch (cause) { setError(cause instanceof Error ? cause.message : "Impossible d’enregistrer la vente."); } finally { setBusy(false); } }
  return <section><div><p className="text-xs font-black uppercase tracking-[0.18em] text-[var(--secondary)]">Power · {label}</p><h1 className="mt-3 text-4xl font-black tracking-[-0.04em] text-[var(--primary)]">Vente de prestations</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-[var(--muted)]">Enregistrez les prestations et suivez les encaissements de la caisse {label} sans mouvement de stock.</p></div>{(error || message) && <p role={error ? "alert" : "status"} className={`mt-6 rounded-lg px-4 py-3 text-sm font-bold ${error ? "bg-[#ffdad6] text-[var(--danger)]" : "bg-[var(--accent-soft)] text-[var(--primary)]"}`}>{error || message}</p>}<div className="mt-8 grid gap-6 xl:grid-cols-[390px_1fr]"><form onSubmit={submit} className="h-fit rounded-2xl bg-[var(--primary)] p-6 text-white"><p className="text-xs font-black uppercase tracking-[0.15em] text-white/55">Nouvelle vente</p><label className="mt-6 block text-sm font-bold text-white/80">Client<input required value={customerName} onChange={(event) => setCustomerName(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-white/15 bg-white/10 px-3 text-white" placeholder="Nom du client" /></label><label className="mt-4 block text-sm font-bold text-white/80">Prestation<select value={serviceId} onChange={(event) => chooseService(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-white/15 bg-white/10 px-3 text-white"><option value="">Choisir une prestation</option>{services.map((item) => <option key={item.id} value={item.id} className="text-black">{item.name} · {money(item.price_xof)}</option>)}{!services.length && fallback.map((item) => <option key={item.name} value="" className="text-black">{item.name} · {money(item.price)}</option>)}</select></label><label className="mt-4 block text-sm font-bold text-white/80">Prix unitaire<input required type="number" min="0" value={price} onChange={(event) => setPrice(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-white/15 bg-white/10 px-3 text-white" /></label><label className="mt-4 block text-sm font-bold text-white/80">Quantité<input required type="number" min="1" value={quantity} onChange={(event) => setQuantity(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-white/15 bg-white/10 px-3 text-white" /></label>{activityCode === "GYM" && <label className="mt-4 block text-sm font-bold text-white/80">Échéance abonnement<input type="date" value={expires} onChange={(event) => setExpires(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-white/15 bg-white/10 px-3 text-white" /></label>}{activityCode === "LODGING" && <label className="mt-4 block text-sm font-bold text-white/80">Durée (minutes pour un pass)<input type="number" min="1" value={duration} onChange={(event) => setDuration(event.target.value)} className="mt-2 h-11 w-full rounded-lg border border-white/15 bg-white/10 px-3 text-white" /></label>}<div className="mt-6 flex items-center justify-between border-t border-white/15 pt-5"><span className="text-sm text-white/60">Total</span><strong className="text-2xl">{money(total)}</strong></div><button disabled={busy || total <= 0} className="mt-5 h-11 w-full rounded-lg bg-[var(--secondary-container)] text-sm font-black text-[var(--primary)] disabled:opacity-50">{busy ? "Enregistrement…" : "Encaisser la prestation"}</button></form><div className="rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-6"><div className="flex flex-wrap items-end justify-between gap-3 border-b border-[var(--line)] pb-5"><div><p className="text-xs font-black uppercase tracking-[0.15em] text-[var(--muted)]">Journal de caisse</p><h2 className="mt-2 text-xl font-black text-[var(--primary)]">Ventes récentes</h2></div><p className="text-sm font-bold text-[var(--muted)]">{money(sales.reduce((sum, item) => sum + Number(item.total_amount_xof), 0))}</p></div>{sales.length ? <div className="divide-y divide-[var(--line)]">{sales.map((sale) => <article key={sale.id} className="flex flex-wrap items-center justify-between gap-4 py-5"><div><p className="font-black text-[var(--primary)]">{sale.customer_name}</p><p className="mt-1 text-xs text-[var(--muted)]">{new Date(sale.created_at).toLocaleString("fr-FR")} · {sale.quantity} × {money(sale.unit_price_xof)}</p>{sale.membership_expires_at && <p className="mt-1 text-xs font-bold text-[var(--secondary)]">Échéance : {new Date(sale.membership_expires_at).toLocaleDateString("fr-FR")}</p>}</div><strong className="text-lg text-[var(--primary)]">{money(sale.total_amount_xof)}</strong></article>)}</div> : <div className="py-16 text-center text-sm text-[var(--muted)]">Aucune vente enregistrée pour cette activité.</div>}</div></div></section>;
}
